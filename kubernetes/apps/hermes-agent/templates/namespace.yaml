apiVersion: v1
kind: Namespace
metadata:
  name: hermes-agent
  labels:
    # s6 image run non-root (fsGroup does its volume chown), so the pod meets restricted
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
