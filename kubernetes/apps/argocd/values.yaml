argo-cd:
  configs:
    params:
      server.insecure: true
  applications:
    app-of-apps:
      project: default
      source:
        repoURL: git@gitlab.com:michael-muzafarov/homelab.git
        targetRevision: main
        path: kubernetes/apps/app-of-apps
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
