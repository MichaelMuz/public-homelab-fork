# ServerSideApply adds defaulted fields (group, kind, weight, matches) to the
# live resource. Without them here, ArgoCD sees a perpetual diff. Keep verbose.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg-private
      namespace: envoy-gateway-system
      sectionName: http-admin
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg-private
      namespace: envoy-gateway-system
      sectionName: https-admin
  hostnames:
    - "grafana.admin.michaelmuzafarov.dev"
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: monitoring-grafana
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
